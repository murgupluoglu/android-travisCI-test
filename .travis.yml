language: android
jdk: oraclejdk8
sudo: false

android:
  components:
    - tools
    - platform-tools
    - tools
    - build-tools-26.0.2
    - android-26

  licenses:
    - 'android-sdk-license-.+'
    - '.+'

  notifications:
    email: false

before_install:
  - mkdir "$ANDROID_HOME/licenses" || true

script:
  - ./gradlew build connectedCheck --info
